apiVersion: v1
kind: Service
metadata:
  name: pay
spec:
  selector: { app: pay }
  ports:
  - name: http
    port: 8000
    targetPort: 8000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pay
spec:
  replicas: 1
  selector: { matchLabels: { app: pay } }
  template:
    metadata:
      labels: { app: pay }
    spec:
      containers:
      - name: app
        image: zt-demo-fastapi:1.0
        imagePullPolicy: IfNotPresent
        env:
        - { name: SERVICE_NAME, value: "pay" }
        - { name: EXP_ID, value: "exp-insecure-001" }
        ports:
        - containerPort: 8000
